2.  In each Git repository where you want to use Git LPS. select the ñle types you'd like Git LFS to manage (or directly edit your .gitattribute5). You can configure additional ñle extensions at anytíme.
